import 'package:flutter/material.dart';
import 'package:p01/mehta/pratice.dart';

void main() => runApp(MareApp());

class MareApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
      return MaterialApp(
        title: "hello",
        home: Pratice(),
      );
  }
}
