#include <stdio.h>

int main()
{

    int a, b;

    printf("Enter Two Numbers :");
    scanf("%d , %d ", &a, &b);

    a += b; // a = a+b;
    printf("value of a after += : %d\n", a);

    // etc .

    return 0;
}
