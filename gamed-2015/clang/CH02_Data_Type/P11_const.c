#include<stdio.h>

int main() {

    const max = 100; // cannot be changed the value 
    printf("Max Value : %d\n", max);
    
    // max = 200;
    // printf("New Max Value : %d", max);

    return 0;
}